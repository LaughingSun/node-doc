'use strict';

/**
 * Parses a documentation object into HTML.
 */
function prepareHTML (doc) {
  throw new Error('Converting documentation into HTML is not done yet');
}

module.exports = prepareHTML;
